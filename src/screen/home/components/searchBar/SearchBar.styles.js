import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  container: {
    backgroundColor: '#ffff',
    borderRadius: 50,
    flexDirection: 'row',
    alignItems: 'center',
    paddingLeft: 10,
  },
  inputText: {
    color: 'grey',
  },
});
