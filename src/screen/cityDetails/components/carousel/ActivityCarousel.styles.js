import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
    gap: 5,
  },
  activityImage: {
    width: 55,
    height: 55,
    borderWidth: 2,
    borderColor: 'white',
    borderRadius: 50,
  },
  activityText: {
    color: '#000',
  },
});
